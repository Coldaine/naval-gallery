[project]
name = "naval-gallery"
version = "0.1.0"
description = "Image harvesting pipeline for naval technical drawings"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "internetarchive>=3.5.0",
]

[tool.uv]
dev-dependencies = []
